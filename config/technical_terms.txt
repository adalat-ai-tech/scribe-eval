# Technical Domain Configuration
# Technical abbreviations and version identifiers

# Metadata
@name: technical
@category: TECH
@label: TER
@case_sensitive: true

# Literal terms - common technical abbreviations
# Case-sensitive to distinguish API from "api" (casual usage)
API
SDK
CLI
JSON
XML
HTTP
HTTPS
REST
SOAP
JWT
SQL
NoSQL
HTML
CSS
URL
URI

# Regex patterns - version numbers
REGEX: v\d+\.\d+(?:\.\d+)?
REGEX: \d+\.\d+\.\d+

# Pattern explanations:
# v\d+\.\d+(?:\.\d+)? matches: v1.0, v2.3.4, v10.15.2
# \d+\.\d+\.\d+ matches: 1.0.0, 2.3.4 (semantic versioning)
